[pytest]
markers =
    integration: real-socket/network integration tests (slower; opt in with -m integration, opt out with -m "not integration")
    requires_real_mt5: requires a reachable real MT5 bridge/terminal (Windows VM); never runs by default. Opt in with -m requires_real_mt5. See PHASE5A_STATUS.md.
